server {
    listen 80;
    server_name pet.loc;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    # Для Let's Encrypt challenge (позже)
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }
}
